name: test-artifact
on: workflow_dispatch
jobs:
  make-artifact:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello" > test.txt
      - uses: actions/upload-artifact@v4
        with:
          name: test-artifact
          path: test.txt
